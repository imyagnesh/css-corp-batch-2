import React from 'react';
import ReactDOM from 'react-dom';
import NameGreet from './app';

ReactDOM.render(
  <NameGreet userName="Akshaya Ganesan" buttonName="Greet" />,
  document.getElementById('root'),
);
