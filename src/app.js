export const a = 'hello from a';

export const b = 'hello from b';

export const c = 'hello from c';

const sum = (p, q) => p + q;

// per file only one export default will be done
export default sum;
